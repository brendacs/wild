import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  container: {
    width: 300,
    height: 50,
    margin: 5
  },
  input: {
    borderWidth: 1,
    borderStyle: 'solid',
    borderColor: 'gray',
    fontSize: 16,
    padding: 12,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 5,
  },
});
